using UnityEngine;
using System.Collections;

public class PowerUp : MonoBehaviour {
    

    //what i wana do with this script is
    //use random to give all the bomberman powerups a number 
    //and have random select a number
    //if the number selected is the power ups actual number than i want 
    // this script to disbale itself and turn into the number that was selected

        //u brused my soul

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
